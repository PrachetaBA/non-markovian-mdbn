# Get the corresponding hypoexponential distribution parameters for the given gamma distribution parameters

# Plotting 

# Generating the cross entropy

# Create the hypoexp_simulator.yaml file from the gamma distributions

# Define query workload for all gamma distribution parameters that we will work with
